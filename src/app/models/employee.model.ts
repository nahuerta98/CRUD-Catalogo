import { EmailValidator } from '@angular/forms';

export class EmployeeModel {
    id: string;
    numEmpleado:string;
    nombre: string;
    email: string;
    telefono: number;
    oficina: string;
}